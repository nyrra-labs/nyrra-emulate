You are operating in a clean git worktree for /home/anandpant/Development/nyrra/nyrra-emulate on branch claude/docs-platform-refactor.

Your job is to implement the docs-platform and Foundry maintainability plan end to end in this worktree, with small verified commits, without pushing.

Context and goals

The human wants a saner architecture with these constraints:

1. Keep one source of truth for inherited upstream docs.
2. Do not proxy the docs site to emulate.dev.
3. Do not deploy the upstream Next app.
4. Keep upstream pulls low-friction.
5. Make Foundry first-class in the docs experience.
6. Support future Foundry growth to 300+ APIs.
7. Clean up docs sprawl, excessive source-file commentary, and stale planning artifacts as part of the same effort.

Decisions already made

1. apps/web stays in the repo for now as the read-only upstream mirror.
2. apps/web-svelte is the only deployed docs app.
3. apps/web-svelte must stop importing apps/web directly.
4. Introduce an owned ingestion boundary, likely packages/docs-upstream.
5. Homepage becomes fully local.
6. Foundry docs become fully local and can grow into a nested hierarchy.
7. Keep one public @emulators/foundry package for now, but modularize it internally by domain.
8. Move architecture rationale out of giant code comments and into proper docs.

Mandatory repo rules

1. Read and follow /home/anandpant/AGENTS.md and /home/anandpant/Development/nyrra/nyrra-emulate/AGENTS.md before editing.
2. Use pnpm for package management.
3. No emojis anywhere.
4. Never use -- as a prose dash. CLI flags are fine.
5. When a change affects how humans or agents use emulate, update the appropriate human-facing docs. Respect the new architecture that apps/web is an upstream mirror and apps/web-svelte is the deployed docs app.
6. Commit frequently after each verified slice. Do not push.

High-level target architecture

1. apps/web remains an upstream mirror only.
2. packages/docs-upstream becomes the owned, generated contract extracted from apps/web.
3. apps/web-svelte consumes only packages/docs-upstream for upstream-backed content.
4. apps/web-svelte owns local shell content, homepage, Foundry docs, search/nav/metadata integration, and deployment.
5. @emulators/foundry remains the public package, but its internals are split by domain and backed by a route registry where helpful.
6. Architecture and contributor guidance live in docs/architecture and docs/contributing rather than giant source headers.

Required implementation plan

Phase 0: policy and guardrails

1. Add architecture docs under docs/architecture describing:
   - docs platform ownership and boundaries
   - apps/web mirror policy
   - docs-upstream ingestion boundary
   - Foundry package/domain boundaries
2. Add contributor docs under docs/contributing describing:
   - upstream docs sync workflow
   - local docs authoring workflow
   - docs deployment workflow
3. Replace or retire stale planning docs in the repo root if they are superseded.
4. Add a guardrail that fails if apps/web-svelte imports apps/web directly outside the sync package boundary.

Phase 1: packages/docs-upstream

1. Create packages/docs-upstream.
2. Implement a sync script that reads the upstream mirror in apps/web and generates committed artifacts such as:
   - a docs manifest
   - copied raw MDX or normalized content assets for inherited pages
   - nav/grouping data
   - relevant upstream metadata and attribution data
3. Add root scripts like docs:sync and docs:sync:check.
4. Commit generated artifacts to git.
5. Ensure CI-friendly determinism.

Phase 2: decouple apps/web-svelte from apps/web

1. Remove direct imports from apps/web into apps/web-svelte.
2. Make apps/web-svelte consume only packages/docs-upstream plus its own local docs/content.
3. Unify search, nav, metadata, and docs registry logic around a local docs layer that merges upstream-backed and local pages.
4. Preserve or improve existing verification coverage.

Phase 3: make homepage fully local

1. Replace the current upstream-root slicing approach.
2. Delete or retire helper code that slices apps/web/app/page.mdx into fragments for the homepage.
3. Keep Foundry-first messaging and upstream attribution in the local homepage.

Phase 4: scalable docs routing and content model

1. Move from flat single-segment assumptions to a true docs path model that can support nested local Foundry pages.
2. Support at least two source kinds in the docs registry:
   - upstream
   - local
3. Keep prerendering and Cloudflare deployment compatibility in good shape.

Phase 5: local Foundry docs tree

Implement a Foundry docs hierarchy that is local to apps/web-svelte and can grow. A good starting IA is:

- /foundry
- /foundry/getting-started
- /foundry/auth/oauth
- /foundry/auth/client-credentials
- /foundry/auth/refresh-tokens
- /foundry/auth/scopes
- /foundry/auth/current-user
- /foundry/admin/enrollments
- /foundry/admin/users
- /foundry/connectivity/connections
- /foundry/connectivity/secrets
- /foundry/ontologies/overview
- /foundry/ontologies/queries
- /foundry/compute-modules/overview
- /foundry/compute-modules/runtimes
- /foundry/compute-modules/jobs
- /foundry/compute-modules/contour
- /foundry/reference/seed-config
- /foundry/reference/endpoints
- /foundry/reference/scope-matrix

You may adjust the exact IA if a slightly better shape emerges, but keep it nested and scalable.

Phase 6: Foundry route registry and generated reference output

1. Add a route registry or capability registry for Foundry runtime support.
2. Use it to generate at least some of:
   - endpoint reference content
   - scope matrix content
   - coverage assertions
3. Prefer machine-readable metadata over giant hand-maintained prose tables.

Phase 7: modularize @emulators/foundry internally

1. Keep one public package: @emulators/foundry.
2. Split internals by domain, for example:
   - auth
   - admin
   - connectivity
   - ontologies
   - compute-modules
   - graphql later if needed
3. Align tests and helpers to the same domain boundaries where practical.
4. Do not over-publish new npm packages unless there is a strong reason.

Phase 8: docs cleanup

1. Trim excessive source-file commentary, especially long headers that narrate architecture better expressed in docs.
2. Keep comments only where they explain invariants, compatibility behavior, or real traps.
3. Consolidate human-facing guidance into the right homes:
   - README for top-level usage and summary
   - docs/architecture for architecture
   - docs/contributing for maintenance flows
   - apps/web-svelte local docs pages for product docs
4. Remove stale root planning artifacts once replaced by durable docs.

Phase 9: final polish and verification

1. Ensure the new architecture is documented.
2. Ensure the import boundary is enforced.
3. Ensure search, nav, metadata, prerendering, and Cloudflare deployment still work.
4. Ensure the repo is left with a coherent commit history on this branch.

Important implementation preferences

1. Minimize or eliminate local edits inside apps/web. Treat it as a mirror.
2. Prefer generated manifests and registries over repeated hand-maintained lists.
3. Keep the docs platform easy to reason about.
4. If you need new dependencies, use pnpm and follow repo rules.
5. If a large phase proves too big, split it into smaller verified commits and continue.

Verification expectations

Run relevant commands after each slice and include them in commit messages or summaries as appropriate. Use at least the commands that fit the touched area, including:

- pnpm --filter web-svelte type-check
- pnpm --filter web-svelte build
- pnpm --filter web-svelte lint
- pnpm --filter web-svelte test
- pnpm --filter @emulators/foundry test
- pnpm build
- pnpm test

Use judgment to avoid wasting time rerunning everything after tiny non-code-only changes, but verify each slice appropriately and do a stronger final verification pass.

Definition of done

1. apps/web remains present but is clearly a mirror-only input.
2. apps/web-svelte has zero direct imports from apps/web.
3. packages/docs-upstream exists and is the upstream ingestion boundary.
4. Homepage is fully local.
5. Foundry docs are local and nested.
6. Search/nav/metadata run on a unified registry for upstream plus local docs.
7. Foundry reference output is generated from a registry where practical.
8. @emulators/foundry is internally modularized by domain while staying one public package.
9. Excessive architectural docstrings are cleaned up.
10. Durable architecture and contributor docs replace stale planning notes.

Working style

1. Start by reading the AGENTS files and auditing the current docs architecture.
2. Make a concise implementation plan for yourself.
3. Execute in small slices with frequent local commits.
4. Do not push.
5. If you hit a blocker, stop with a clear summary of what is done and what remains.

When you finish, leave:

1. the branch updated with local commits only
2. a final summary in the session output
3. exact verification commands and outcomes
4. any known follow-up items if something remains intentionally deferred
