node_modules/
dist/
*.tsbuildinfo
.turbo/
.pnpm-store/
emulate.config.yaml
emulate.config.yml
.env*
!.env.example

# emulate CLI README is copied from root by prepack
packages/emulate/README.md

# opensrc - source code for packages
opensrc/

# Claude Code local settings (hooks, per-machine state). The Claude-facing
# repo guidance lives in the tracked CLAUDE.md at the repo root, not here.
/.claude/
