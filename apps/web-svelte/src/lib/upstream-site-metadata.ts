/**
 * Re-exports of upstream branding constants from the docs-upstream
 * generated package.
 *
 * Only the constants the Svelte side actually uses are re-exported.
 * FoundryCI-specific facts stay local in page-metadata.ts.
 */
export {
  GITHUB_REPO_URL,
  NPM_PACKAGE_URL,
  OG_IMAGE_HEIGHT,
  OG_IMAGE_WIDTH,
  OG_LOCALE,
  OG_TYPE,
  PAGE_SITE_DESCRIPTION,
  SITE_NAME,
  TWITTER_CARD,
  ogImageAlt,
  suffixWithSiteName,
} from "docs-upstream";
